/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    let a = arr.length;
    let newArr = new Array(a);
    for(let i = 0; i<a; i++){
        newArr[i] = fn(arr[i],i);
    }
    return newArr;
};
